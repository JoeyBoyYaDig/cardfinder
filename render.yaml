services:
  - type: web
    name: cardfinder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: POKEMON_TCG_API_KEY
        sync: false
      - key: EBAY_APP_ID
        sync: false
